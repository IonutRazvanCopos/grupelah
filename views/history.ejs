<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event History</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/history.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Slujiri</h1>
        <h3 class="text-center">Am avut <%= totalEvents %> slujiri</h3>
        <ul class="list-group mt-4">
            <% events.forEach(event => { %>
                <li class="list-group-item">
                    <strong><%= new Date(event.date).toLocaleDateString('ro-RO', { day: '2-digit', month: 'long', year: 'numeric' }).replace(/^\w/, c => c.toUpperCase()).replace(/(\s\w)/, c => c.toUpperCase()) %></strong> - <%= event.content %>
                </li>
            <% }); %>
        </ul>
        <a href="/" class="btn btn-primary mt-4">Back to Home</a>
    </div>
</body>
</html>