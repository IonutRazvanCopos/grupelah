<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Slujiri</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/history.css">
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">2025</h1>
        <h3 class="text-center">Grup Elah a avut <%= totalEvents %> slujiri</h3>
        <ul class="list-group mt-4">
            <% events.forEach(event => { %>
                <li class="list-group-item">
                    <strong id="text"><%= new Date(event.date).toLocaleDateString('ro-RO', { day: '2-digit', month: 'long', year: 'numeric' }).replace(/^\w/, c => c.toUpperCase()).replace(/(\s\w)/, c => c.toUpperCase()) %></strong>-
                     <span class="event-content"><%= event.content %></span>
                </li>
            <% }); %>
        </ul>
        <a href="/" class="btn btn-primary mt-4" id="backHome">BACK</a>
    </div>
</body>
</html>
